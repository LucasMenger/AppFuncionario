services:
  api:
    image: myapp-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_USER: as
      POSTGRES_PASSWORD: fsdfh1823d1dasdh123
      POSTGRES_DB: newDb2
    ports:
      - "5432:5432"
    
  
        
